org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.daemon=false           # Disable Gradle daemon (saves RAM)
org.gradle.parallel=false         # Disable parallel builds (reduces CPU/RAM spikes)
android.useAndroidX=true
android.enableJetifier=true
